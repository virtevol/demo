version: 2
jobs:
  build:
    #...
  deploy:
    machine:
      enabled: true
    steps:
    steps:
      - add_ssh_keys:
          fingerprints:
            - "6e:0e:61:15:23:b0:54:b7:e3:d8:7e:15:97:88:df:59"
      - run:
          name: Deploy Over SSH
          command: |
            - scp -r * comi9@require.ro:/var/www/html/

workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build
          filters:
            branches:
              only: master
