version: 2
jobs:
   build:
     docker:
       - image: circleci/node:latest
     steps:
      - add_ssh_keys:
          fingerprints:
            - "bd:96:6a:d8:d8:12:f2:3d:91:32:cf:35:c9:69:75:5d"
      - run: echo "WE'RE ONLINE"        
      - run: scp -r * comi9@require.ro:/var/www/html/
      - run: echo "Completed"
      
workflows:
  version: 2
  deploy:
    jobs:
      - build:
          filters:
            branches:
              only:
                - master
