version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.6.2-stretch-browsers
    steps:
      - checkout

test:  
  override:
    - "true"
    
deployment:
  production:
    branch: master
    commands:
      - scp -r * comi9@require.ro:/var/www/html/
