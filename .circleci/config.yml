version: 2
jobs:
   build:
     docker:
       - image: circleci/node:latest
     steps:
       - run: echo "WE'RE ONLINE"
       - run: scp -r * comi9@require.ro:/var/www/html/
       - run: echo "Yes"

workflows:
  version: 2
  deploy:
    jobs:
      - build:
          filters:
            branches:
              only:
                - master
